# Normalize all text files to LF in the repository and in working trees.
* text=auto eol=lf

# CSV/TSV fixtures are byte-exact test data: their line endings, encodings
# and BOMs are the very thing under test, so git must never rewrite them.
*.csv -text diff
*.tsv -text diff
